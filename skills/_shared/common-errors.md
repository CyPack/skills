# Cross-Skill Ortak Hatalar

| Error Pattern | Gecerli Skill'ler | Fix | Kaynak |
|---------------|-------------------|-----|--------|
| MCP server timeout | Tum MCP skill'ler | MCP restart, retry | voorinfra |
| Docker inter-container `fetch failed` / bağlanamıyor | soulforge-mcp, voorinfra-mcp, herhangi Docker servis | İki container farklı network'te — `localhost:PORT` container içinden ulaşılamaz. FIX: hedef container'ı kaynak network'e ekle (`external: true` + `name: network_adi`) VEYA URL'de container adını kullan (`http://container-name:PORT`) | cross-skill 2026-04-11 | [H] |
| ScraplingServer auth/proxy_auth TypeError | ScraplingServer MCP 0.4.x | 0.3.x→0.4.x breaking: `auth=("user","pass")` → `auth={"username":"user","password":"pass"}`. proxy_auth da aynı. | ScraplingServer |
| ScraplingServer bulk_* urls tuple hatası | ScraplingServer MCP 0.4.x | `urls=("url1","url2")` → `urls=["url1","url2"]` (list kullan) | ScraplingServer |
| ScraplingServer cookies/params format hatası | ScraplingServer MCP 0.4.x | cookies: dict only (list of tuple artık kabul edilmiyor). params: dict only. | ScraplingServer |
| 401 Unauthorized | Tum API skill'ler | Re-auth, credential kontrol | dokploy-manage |
| Connection refused | Tum MCP/API | Servis durumu kontrol | t4f-scraper |
| asyncio daemon thread: "Event loop is closed" 2. çağrıda | Python asyncio daemon thread içinde `asyncio.run()` event loop'u kapatıyor; paylaşılan httpx client ikinci çağrıda kapalı loop'a bağlı → crash | Her `asyncio.run()` çağrısında `async def _run_fresh(): inst = ServerClass(); return await inst.method()` ile fresh instance oluştur. Thread-shared `self` instance kullanma | voorinfra-upload 2026-03-07 | [H] |
| ENOENT file not found | Dosya bazli | Path dogrula, input dir kontrol | voorinfra |
| Rate limit exceeded | API skill'ler | Backoff, bekle | site-analyzer |
| Base64 decode error | Import tool'lari | UTF-8 → base64 kontrol | dokploy-manage |
| MCP config degisikligi etkisiz | Tum MCP skill'ler | Claude Code deferred tools listesi session basinda olusturulur. Config degistirdikten sonra Claude Code'u tamamen kapat-ac. `claude mcp get` "Connected" gosterse bile eski process calisiyor olabilir | dokploy-manage |
| MCP tool disabled | xmcp-based MCP'ler | `--enable-tools category/` args ekle + env'e `XMCP_ENABLE_TOOLS=category/` ekle + session restart | dokploy-manage |
| Docker internal IP degisti (ECONNREFUSED) | Tum Docker-based MCP'ler | Container restart IP degistirir. `docker inspect {container} --format '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}'` ile yeni IP bul. `~/.claude.json` guncelle. Mid-session: `docker exec` alternatif kullan | supabase-selfhosted |
| RLS infinite recursion (42P17) | supabase-selfhosted | Self-referencing subquery RLS policy'de sonsuz dongu yaratir. Direct user_id check kullan, subquery'de ayni tabloyu referans verme | supabase-selfhosted |
| Custom command var ama tetiklenmiyor | GSD workflow entegrasyonu | Command dosyasi olusturmak ≠ entegrasyon. 5 katman kontrol: (1) command dosyasi, (2) workflow pipeline step, (3) agent prompt uyumu, (4) config toggle + gsd-tools.js init parsing, (5) docs guncelleme. Eksik olan katmani bul | gsd-enhanced |
| gsd-tools.js init yeni field dondurmuyor | GSD config | loadConfig defaults'a yeni field ekle + init fonksiyonunda result object'e ekle + `?? true` fallback kullan | gsd-enhanced |
| Planner TDD/behavior yazmadi | GSD planner prompt | Agent prompt'unda "SHOULD" degil "MUST/MANDATORY" kullan. Detection → Enforcement. Oneri degil zorunluluk | gsd-enhanced |
| `~/.mcp.json` ile `~/.claude.json` MCP config cakismasi | Tum MCP skill'ler | `~/.mcp.json` project-scoped config `cwd` desteklenmez → server crash → tools kaybolur. COZUM: `~/.mcp.json`'dan sil, `~/.claude.json` global config kullan. ASLA `-m module` + `cwd` kullanma, mutlak dosya yolu kullan | t4f-scraper |
| Agent HALLUCINATED data (MCP tools erisilemedigi icin) | Tum MCP-based agent'lar | MCP tools unavailable olunca agent veri UYDURUYOR. Belirtiler: tam yuvarlak sayilar, tutarsiz tarihler, UUID/adres yok, haftasonu verisi. COZUM: Once MCP tool erisimi dogrula (ToolSearch), sonra veri cek. Erisim yoksa HEMEN raporla, uydurma | t4f-scraper |
| MCP server stdout+stderr'e ayni cikti yaziyor | stdio-based MCP server'lar | Bazi MCP server'lar JSON response'u hem stdout hem stderr'e yaziyor. Claude Code stderr'deki JSON'u hata olarak yorumluyor ve baglanti KURULAMIYOR. COZUM: `~/.claude.json` config'de command'i `bash` yap, args'a `["-c", "node /path/to/server.js 2>/dev/null"]` ekle. DIKKAT: `exec node ... 2>/dev/null` CALISMAZ, `bash -c` gerekli | excalidraw-mcp |
| Docker canvas sync basarisiz ama "synced" diyor | MCP + Docker canvas | MCP server "syncedToCanvas:true" dondurur ama canvas container'da element gorunmez. MCP server kendi in-memory store'una yaziyor, canvas REST API'sine sync guvenilmez. COZUM: Canvas REST API'sine dogrudan `curl -X POST /api/elements` ile push et | excalidraw-mcp |
| Penpot MCP execute_code timeout (Running...) | Penpot MCP (HTTP transport) | Plugin WebSocket baglantisi kopmus. Penpot'ta Plugin penceresi ACIK olmali. Reconnect: Plugins(Ctrl+Alt+P) -> Open -> Connect. Plugin kapatilirsa/sayfa refresh olursa baglanti kopar | penpot-mcp |
| Penpot MCP "Server not initialized" | Penpot MCP (HTTP transport) | MCP server restart edilmis ama Claude Code eski session'a bagli. COZUM: Claude Code restart (Ctrl+C -> claude). Siralama: 1) MCP server up 2) Plugin connect 3) Claude Code fresh start | penpot-mcp |
| Penpot plugin EMFILE hatasi (vite watch) | Penpot MCP kurulum | `vite build --watch` cok fazla file watcher aciyor. COZUM: Watch mode yerine `npx tsc && npx vite build` ile static build yap, sonra `npx vite preview --port 4400` ile serve et | penpot-mcp |
| Penpot headless MCP export_shape yok | penpot-headless (zcube) | zcube MCP'de export tool yok. WORKAROUND: curl ile Penpot export API (`/api/rpc/command/export`) cagir, veya Plugin MCP fallback kullan. Headless mode %95 kullanim icin yeterli | penpot-mcp |
| Penpot Docker 502 Bad Gateway (restart sonrasi) | penpot-headless + plugin | Backend startup 10-15sn suruyor. `curl -s -o /dev/null -w "%{http_code}" http://localhost:9001/api/rpc/command/get-profile` ile health check, 200 donene kadar bekle | penpot-mcp |
| Penpot Access Token 401 (headless) | penpot-headless | Token gecersiz/expired. Penpot UI → Settings → Access Tokens → yeni olustur. `~/.claude.json` env + `~/penpot/.penpot-credentials` guncelle. Claude Code restart | penpot-mcp |
| 429 Rate Limit persistent (free plan) | Figma, tum rate-limited API'ler | Free plan'da 429 dakikalarca surebilir. Backoff CALISMAZ. WORKAROUND: Image export (ayri rate limit bucket), visual inspection ile devam et. Paralel API cagrisi YAPMA | figma-mcp |
| file:// protocol blocked (browser) | Playwright + lokal HTML | Chromium file:// URL'leri bloklar. COZUM: `python3 -m http.server {port}` ile localhost serve et, `http://localhost:{port}` kullan | figma-mcp |
| Playwright getByRole strict mode violation | Playwright + cok elemanlı UI | `getByRole('button')` birden fazla match bulursa strict mode FAIL. COZUM: `getByTestId()`, `nth()`, veya `filter({hasText:})` kullan. Penpot/Figma Community sayfalarinda ozellikle dikkat | figma-mcp |
| Playwright stale element reference | Playwright + SPA navigasyon | SPA navigasyondan sonra eski element referanslari invalid. COZUM: Navigasyon sonrasi `waitForLoadState('networkidle')` + element'i TEKRAR locate et | figma-mcp |
| MCP tool "Unexpected end of JSON input" | Tum MCP API tool'lari | API yanit parse edilemiyor, genellikle deployment/durma/islem sonrasi. COZUM: Status kontrolu icin READ tool kullan (application-one, compose-one), retry gerekirse | dokploy-manage |
| Docker container env variables not applied via MCP | Docker-deploy MCP tool'lari | MCP saveEnvironment/database config calisiyor gorunuyor ama container'a iletilmiyor. COZUM: Manuel docker-compose ile deploy et, MCP bypass | dokploy-manage |
| DBgate "Engine driver undefined not found" / "missing ENGINE" | DBgate Community Edition docker-compose | Env var prefix YANLIŞ: `CONN_CON1_ENGINE` ÇALIŞMAZ. `extractConnectionsFromEnv()` `ENGINE_{ID}` key'i okur. FIX: `ENGINE_CON1=postgres@dbgate-plugin-postgres`, `SERVER_CON1`, `USER_CON1`, `PASSWORD_CON1`, `PORT_CON1` formatını kullan | dokploy-manage [H] |
| Compose file in-disk güncellendi ama Dokploy UI/MCP eski hali gösteriyor | Tüm Dokploy compose servisleri | Disk'teki docker-compose.yml ve Dokploy DB'deki composeFile birbirinden bağımsız. Disk'i güncellemek Dokploy DB'yi DEĞİŞTİRMEZ. FIX: `psql UPDATE compose SET "composeFile"=...` ile Dokploy DB'yi de güncelle, ardından `compose-deploy` MCP ile yeniden deploy et | dokploy-manage [M] |
| App requires database but MCP doesn't configure it | tum container deploy MCP | database required uygulamalarda (PostgreSQL, MySQL) MCP env config yetersiz. COZUM: compose YAML ile database service ekle, depends_on ile sirala | dokploy-manage |
| Browser localStorage yanlış config (UI "connection failed") | Playwright + web dashboard'lar | Sunucu çalışıyor ama UI "not connected/failed" diyor. NEDEN: Dashboard konfigürasyonu (API key, URL) browser localStorage'da tutulur, server'da değil. curl OK = server sağlıklı = sorun browser katmanı. COZUM: Playwright ile dashboard'a git → config form bul → doğru key/URL yaz → Save. TEST: browser_console_messages'da 401 @ /api/endpoint = localStorage yanlış key | dokploy-manage (WAHA) |
| Playwright ile basic auth gerektiren sayfalara gidilememe | Playwright + basic auth korumalı sayfalar | browser_navigate doğrudan URL'e gidince ERR_INVALID_AUTH_CREDENTIALS. COZUM: Credentials'ı URL'e göm: `http://user:password@host:port/path` | dokploy-manage (WAHA Dashboard) |
| Kullanıcı hata bildirdi ama server OK — AI "çalışıyor" dedi ve Playwright açmadı | Tüm Playwright + deployment skill'ler | AI büyük hata yapıyor. Server tarafı OK = sorun BITMEDI. Browser katmanı (localStorage, JS config, yanlış endpoint) hâlâ sorunlu olabilir. KURAL: Kullanıcı hata bildirdi + server curl OK → HEMEN Playwright → navigate → screenshot → console → network | dokploy-manage |
| docker_gwbridge firewalld ZONE_CONFLICT (Swarm ingress failure) | Tum Docker Swarm skill'ler (dokploy-manage, supabase-selfhosted) | firewalld'da stale interface binding var. `firewall-cmd --get-zone-of-interface=docker_gwbridge` kontrol et. `trusted` yerine `docker` olmali. FIX: `firewall-cmd --zone=trusted --remove-interface=docker_gwbridge --permanent` + runtime + docker_gwbridge recreate | dokploy-manage [H] |
| Docker daemon restart sonrasi Swarm service'ler kayboldu | Tum Docker Swarm skill'ler | docker_gwbridge olusturulamadi → ingress yok → service'ler baslatilmadi. Volume'ler ve config dosyalari saglamdir. Full restore: firewalld fix → network create → secret create → service create (sirali) | dokploy-manage [H] |
| MCP streamable-http healthcheck 406 on GET | Docker MCP server'lar (FastMCP) | FastMCP streamable-http endpoint'i GET kabul etmez, 406 döner. Healthcheck `curl -sf` FAIL olur. COZUM: POST ile health check yap veya ayrı health endpoint ekle. Container çalışıyor ama Docker "unhealthy" gösterir — fonksiyonel sorun YOK | twitter-mcp [M] |
| noVNC'de Chromium penceresi görünmüyor (headless=false) | Docker Patchright + noVNC | Xvfb'de window manager yok, Chromium penceresi offscreen açılabilir. FIX: Chromium args'a `--start-maximized --window-position=0,0 --window-size=1920,1080` ekle | twitter-mcp [M] |
| Docker GPU passthrough nvidia driver version mismatch | Docker + NVIDIA GPU | Host driver 580.126.18 vs container 580.119.02 uyumsuzluğu. `libEGL_nvidia.so` bulunamıyor. FIX: `sudo dnf install golang-github-nvidia-container-toolkit && sudo nvidia-ctk cdi generate --output=/etc/cdi/nvidia.yaml && sudo systemctl restart docker` | dokploy-manage [H] |
| `claude mcp add --url` flag yok | Claude Code MCP config | `--url` parametresi mevcut değil. DOGRU: `claude mcp add --transport http -s user <name> <url>` (URL pozisyonel arg) | twitter-mcp [M] |
| MCP JSON-RPC Accept header gerekli | Docker MCP server'lar (FastMCP) | curl ile JSON-RPC çağrısında `Accept: application/json, text/event-stream` header'ı ZORUNLU. Eksikse 406 Not Acceptable döner | twitter-mcp [M] |
| MCP streamable-http plain JSON-RPC POST 400 döner | Docker MCP FastMCP streamable-http | FastMCP streamable-http endpoint'i düz JSON-RPC POST'u da reddeder (400 Bad Request). Claude Code MCP protokolü farklı (session negotiation). FIX: Healthcheck için `curl -s -o /dev/null http://localhost:{port}/mcp` kullan — -f flag YOK, herhangi bir HTTP yanıtı (406/400) = server çalışıyor. docker-compose healthcheck: `["CMD-SHELL", "curl -s -o /dev/null http://localhost:8010/mcp"]` | twitter-mcp [H] |
| FastMCP "Session not found" after context compaction | Docker MCP FastMCP streamable-http (tüm HTTP MCP'ler) | Context compaction sonrası Claude Code eski session ID'yi gönderiyor, server kabul etmiyor. Belirtiler: tüm MCP tool'lar `{"code":-32600,"message":"Session not found"}` döner, container healthy'dir. FIX: Claude Code restart (Ctrl+C → claude). Container restart GEREKMEZ. Penpot "Server not initialized" ile aynı kök neden | twitter-mcp [H] |
| Docker compose file /tmp'de ise reboot sonrası DNS kaybolur | Tum Docker-compose tabanlı servisler | /tmp reboot'ta temizlenir. Compose olmadan restart eden containerlar Docker DNS alias'larını kaybeder (`postgres`, `redis` gibi service adları NXDOMAIN olur). FIX: `docker network disconnect <net> <c> && docker network connect --alias <svcname> <net> <c>` ile alias geri ekle. KALICI: docker-compose.yml dosyasını `~/` altına taşı | whatsapp-poc [H] |
| Redis AUTH mismatch — container'da requirepass yok ama client şifre gönderiyor | Docker Redis + client | Docker env'de REDIS_PASSWORD var ama redis container'da `requirepass` configure edilmemiş. Belirtiler: `ERR AUTH <password> called without any password configured` (sidekiq/rails log). FIX (volatile): `docker exec <redis> redis-cli CONFIG SET requirepass "<şifre>"`. FIX (kalıcı): docker-compose.yml'de redis service'e `command: redis-server --requirepass <şifre>` ekle | whatsapp-poc [H] |
| Evolution API curl CORS hatası — CORS_ORIGIN kısıtlı | Evolution API + curl | docker-compose.yml'de `CORS_ORIGIN=http://localhost:3000,...` tanımlı. Curl default Origin header göndermez → `{"message":"Not allowed by CORS"}`. FIX: Her curl'e `-H "Origin: http://localhost:8080"` ekle (veya CORS_ORIGIN'e kendi URL'ini ekle) | whatsapp-poc [M] |
| Evolution API Baileys QR üretmiyor — ChannelStartupService silent loop | Evolution API v2.2.3 (Baileys) | Instance oluşturuldu, status "connecting" ama `/instance/connect/{name}` `{"count":0}` dönüyor. Loglar: "Browser version → Baileys version → Group Ignore: false" her ~5s tekrar. İnternet bağlantısı OK. Kök neden: Stale Baileys (Feb 2025, 13ay eski) WhatsApp tarafından bloke edilmiş olabilir. DB bağlantısı sorun değil. IDLE LOOP (instance yok) NORMAL, asıl problem QR=0. DENEMELER SIRASI: 1) `?schema=public` + `QRCODE_LIMIT=30` ENV fix + instance recreate 2) `DATABASE_ENABLED=false` 3) WAHA fallback + webhook relay. | whatsapp-poc [M] |
| Bridge CC spawn — safety firewall "restart" keyword bloğu | Bridge CC spawn | `safety-firewall.sh` JSON payload içindeki "restart" kelimesini sistem reboot sanıyor → curl engelleniyor. FIX: Görevi `/tmp/cc-X-task.md` dosyasına yaz (Write tool), CC'ye `"read /tmp/cc-X-task.md and execute"` kısa prompt gönder. | whatsapp-poc [H] |
| WAHA CORE Chatwoot native entegrasyonu yok | WAHA CORE + Chatwoot | `/api/channels` → 404. Native Chatwoot inbox WAHA PLUS ($19/mo) gerektirir. WORKAROUND: Python webhook relay Flask/FastAPI ~50 satır. | whatsapp-poc [H] |
| Research agent Evolution API Docker Hub versiyonu halüsinasyonu | Evolution API | Agent "v2.3.7 mevcut" dedi — YOK. En güncel v2: `v2.2.3` (2025-02-03). Docker Hub API: `https://hub.docker.com/v2/repositories/atendai/evolution-api/tags/` ile doğrula. | whatsapp-poc [H] |
| X.com React SPA `domcontentloaded` → 0 tweet | Patchright/Playwright + X.com scraping | `domcontentloaded` HTML iskelet demek, tweet'ler GraphQL call'dan sonra gelir (async React render). `page.goto(..., wait_until="domcontentloaded")` + fixed sleep YETERSIZ — race condition. FIX: `await page.wait_for_selector('article[data-testid="tweet"]', state="visible", timeout=30000)` navigation sonrası. `networkidle` KULLANMA — X.com hiç networkidle'a ulaşmaz (background polling). | twitter-mcp [H] |
| Playwright scraping loop `no_new_count` premature exit | Patchright/Playwright + SPA scraping | `no_new_count` ilk iterasyonda saymaya başlarsa ve sayfa henüz yüklenmemişse, 3 boş scroll sonrası döngü erken çıkar (0 tweet). FIX: `page_confirmed_loaded = False` flag — `count > 0` olana kadar `no_new_count` sayma, sadece `continue` ile bekle. | twitter-mcp [H] |
| X.com mid-scroll logout (articles=0, logged_in=False) | Patchright + X.com timeline | Uzun scraping session (bookmarks 500s+) sonrası Twitter sunucu tarafında session'ı expire eder. Sonraki timeline çağrısında `articles=0, logged_in=False` görünür. `twitter_session_status()` ve `twitter_login()` sadece cookie dosyasını kontrol eder — YANLIŞ pozitif döner. FIX: Container restart → `docker compose restart twitter-mcp` → yeni browser process açılır, cookies yeniden okunur. Kalıcı fix: bookmarks ve timeline çağrılarını ayrı session'lara böl, tek session'da maksimum 300s. | twitter-mcp [H] |
| `docker cp` ile container'a kopyalanan dosya image'da değil | Docker container update | `docker cp host_file container:/path` container'a yazar ama image'a değil. Container restart → sıfırlanır, DEĞIL (running container'da kalır). Ama `docker compose up --force-recreate` veya image rebuild + `docker compose up -d` → ESKİ image kullanılır, değişiklik kaybolur. FIX: Kod değişikliği sonrası `docker compose build <service>` ile image rebuild ZORUNLU. `docker cp` sadece hızlı test içindir. | twitter-mcp [M] |
| Docker healthcheck `curl: not found` — container unhealthy ama çalışıyor | `b3log/siyuan` gibi minimal image'larda `curl` yok. Compose veya image HEALTHCHECK `curl` kullanıyorsa unhealthy döner. `docker inspect` → `Health.Log → "Output": "/bin/sh: curl: not found"`. Servis sağlıklı çalışıyor olabilir. | `wget` kullan: `wget -q -O- http://localhost:PORT/api/endpoint \|\| exit 1`. Compose healthcheck: `test: ["CMD-SHELL", "wget -q -O- http://localhost:PORT/health \|\| exit 1"]`. Hangi tool var? `docker exec <container> which curl wget` | tum-docker [M] |
| `docker restart` yeni image'ı uygulamıyor | `docker restart` container'ı durdurur+başlatır ama image ID değişmez. `docker push` sonrası `docker restart` hâlâ ESKİ image çalıştırır. | `docker stop <name> && docker rm <name>` → `docker run ...` ile yeniden oluştur. Sonra `docker ps` ile `IMAGE` sütununda digest/tag doğrula. | tum-docker [H] |
| Bridge CC spawn response JSON parse hatasi (inline pipe) | bridge-dev, openclaw-manage | curl ciktisi python pipe'a newline/kontrol karakterleri icerirse `json.loads()` kirilir. FIX: `> /tmp/resp.json` dosyaya yonlendir, sonra `json.load(open('/tmp/resp.json'))` ile parse et. ASLA inline pipe kullanma | bridge-dev [M] |
| Bridge interceptor keyword false positive (status/compact/temizle) | bridge-dev, bridge-local, openclaw-manage | CC spawn mesajinda standalone 'status', 'compact', 'temizle', 'doktor' → bridge komut olarak yorumlar, CC'ye ulasmaz. FIX: Gorevi `/tmp/cc-task-{ts}.md` dosyasina yaz, CC'ye "dosyayi oku ve uygula" kisa prompt gonder | bridge-dev [H] |
| Bridge restart sonrasi eski kod calisiyor (systemd) | bridge-dev | systemd daemon eski binary'yi kullanir. FIX: `sudo systemctl restart openclaw-bridge` (daemon-reload gerekebilir). `curl ping` ile UP dogrula. ASLA `kill PID` + manual nohup kullanma — systemd yonetsin | bridge-dev [H] |
| Node --experimental-strip-types import `.js` → ENOENT | bridge-dev | `import '../module.js'` → "Cannot find module". `.ts` extension kullan. `.js` CALISMAZ | bridge-dev [H] |
| Node.js 24 IPv6-first `localhost` → `[::1]` — backend IPv4'te → timeout/refused | Vite proxy, Hono, tum Node.js http client | `localhost` yerine explicit `127.0.0.1` kullan. Backend'i `--host 0.0.0.0` ile tum interface'lerde dinlet. `http-proxy` paketi KULLANMA — native `node:http` ile kendi proxy'ni yaz | vite-guide + claude-preview 2026-03-30 |
| Claude Preview MCP `autoPort:true` + Vite = port mismatch | claude-preview + vite-guide | Vite `PORT` env OKUMAZ. `autoPort: false` + `--port 5173` explicit kullan. launch.json'da `runtimeExecutable: "bash"` + shell script wrapper | claude-preview 2026-03-30 |
| Claude Preview MCP `launch.json` CWD-bagimli — proje dizinindeki config gorunmez | claude-preview, tum Preview kullanicilari | `~/.claude/launch.json` (global) kullan. Claude Code CWD ne olursa olsun buraya bakar. | claude-preview 2026-03-30 |
| RR7 `setSearchParams({key:val})` diger param'lari siler | react-router-v7, tum RR7 projeleri | Object form TÜM param'lari replace eder. Callback form kullan: `setSearchParams(prev => { const n = new URLSearchParams(prev); n.set(k,v); return n; })` | react-router-v7 2026-03-31 |
| RR7 useEffect yarisi: iki effect ayni searchParams'a bagli, birbirini eziyor | react-router-v7, tum RR7 projeleri | Yan efektli effect (URL degistiren `setTab`) yerine sadece local state guncelleyen `setActiveTab` kullan. URL param cycle'i kirilir | react-router-v7 2026-03-31 |
| macOS bash 3.2 → `wait -n`, associative arrays, `&>` CALISMAZ | Tum shell script kullanan skill'ler | macOS default `/bin/bash` 3.2. `wait -n` → `wait`, `declare -A` → baska yaklasim. Script'lerde bash 4+ feature KULLANMA veya `#!/opt/homebrew/bin/bash` shebang kullan | cross-skill 2026-03-31 |
| Config sync (mainfedora→macOS) sonrasi path'ler kirilir | Tum multi-device projeler | Linux `/home/ayaz/` → macOS `/Users/ayazmutlu/`. `.claude/launch.json`, `.env`, script path'leri kontrol. COZUM: Relative path tercih et, mutlak path gerektiyorsa env var kullan | cross-skill 2026-03-31 |
| `.env` `NODE_ENV=production` → dev tool'lar bozulur | Vite, Next.js, tum dev framework'ler | `.env`'de `NODE_ENV=production` YAZMA. Dev server'lar sadece `development` kabul eder. Production icin build tool'un otomatik ayari yeterli | cross-skill 2026-03-31 |
| Dual-store desync: iki UI aynı veriye farklı key'lerle yazıyor | Tüm React Context + localStorage projeleri | Bir veri türü için TEK store + TEK UI seç. İkinci UI'den yazma ekleme — sync kodu karmaşıklık + bug kaynağı. Yazma tek noktadan, okuma birden fazla yerden olabilir | cross-skill 2026-04-01 |
| Drag-to-reorder: item ring highlight → arası çizgi olmalı | Tüm drag-reorder UI'lar | `onDragOver`'da `clientY < rect.midY` → üst/alt yarı. dropTarget = insertion INDEX (item index değil). Visual: item'lar arası `h-0.5 bg-primary` çizgi, ring/border highlight KULLANMA | cross-skill 2026-04-01 |
| 28 section visible toggle → sidebar'da 28 API call riski | Tüm configurable section UI'lar | Visible toggle YERINE add/remove pattern kullan (header zones gibi). Mevcut olanlar config array'de, olmayanlar yok. Lazy fetch hala gerekli (accordion modda) ama visible toggle gereksiz karmasiklik — section listede varsa gosterilir, yoksa gosterilmez | cross-skill 2026-04-04 | [M] |
| macOS `cat=bat` alias → `git commit -m "$(cat <<'EOF'..."` fail | Tum git commit islemleri (macOS) | `~/.zshrc`'de `alias cat="bat"` varsa heredoc commit pattern kirilir: `(eval):1: command not found: bat`. COZUM: Heredoc KULLANMA, commit mesajini dogrudan `-m "mesaj"` ile gonder. Cok satirli mesajda newline iceren string literal kullan | cross-skill 2026-04-04 | [H] |
| OwnPilot gateway X-Runtime header forward etmiyor | Gateway + Bridge multi-provider | OwnPilot UI → gateway arasi X-Runtime header bridge'e ILETILMIYOR. Gateway yeni fetch request olusuturuyor. FIX: agent-cache.ts'de loadProviderConfig() icinde local provider name'den (`bridge-*`) X-Runtime header cikarsayip config.headers'a ekle + openai-compatible.ts'de providerId check | cross-skill 2026-04-05 | [H] |
| OpenCode adapter model=undefined → bos response | Bridge multi-provider + OpenCode | OpenCodeRuntimeAdapter.send() model parametresi undefined geciyor → `opencode run --model undefined` → OpenCode model bulamiyor → bos response. FIX: model parametresini omit et (default kullanilir) veya getOrCreate'den session-based model pass et | bridge-dev 2026-04-05 | [H] |
| Node.js --experimental-strip-types TS parameter properties REDDEDER | Tum Node.js TS projeler | `constructor(public readonly x: T)` → `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX` hatasi, process BASLATILMAZ. FIX: Manuel field declaration + constructor assignment: `readonly x: T; constructor(x: T) { this.x = x; }` | cross-skill 2026-04-05 | [H] |
| Local provider UUID vs name: UI provider ID olarak UUID gonderiyor | OwnPilot + herhangi bir local provider | `provider.startsWith('bridge-')` kontrol UUID ile CALISMAZ. FIX: providerNames map'i localStorage'a kaydet, useChatStore'da localStorage'dan oku | cross-skill 2026-04-05 | [H] |
| OwnPilot fetchBridgeModels() null name crash | OwnPilot gateway models.ts | Local provider mock'ta veya DB'de `name` null olabilir → `lp.name.startsWith(...)` TypeError. FIX: optional chaining `lp.name?.startsWith('bridge-')` | cross-skill 2026-04-05 | [M] |
| Docker restart vs stop+rm+run — yeni image uygulanmiyor | Tum Docker container deploy'lar | `docker restart` container'i ayni image ile yeniden baslatir — YENI image'i KULLANMAZ. `docker stop X && docker rm X && docker run ... new-tag` ZORUNLU. `docker ps` IMAGE sutununda yeni tag'i DOGRULA | cross-skill 2026-04-05 | [H] |
| OwnPilot UI password ≠ API key — farkli auth mekanizmalari | OwnPilot + Playwright UI test | UI login (`POST /auth/login`) bcrypt hash kontrol eder. API auth (`Authorization: Bearer`) timing-safe key match yapar. API key ile UI'ya giris YAPILAMAZ. UI password sifirlamak icin DB'den hash sil + container restart | cross-skill 2026-04-05 | [H] |
| settingsRepo in-memory cache — DB degisikligi hemen yansimaz | OwnPilot gateway (tum settings) | DB'den direkt `DELETE/UPDATE` yapildiginda gateway'in in-memory cache'i STALE kalir. Container restart (cache sifirlanir) VEYA settings API uzerinden guncelle. ASLA sadece DB degistirip "calismadi" deme | cross-skill 2026-04-05 | [M] |
| Playwright React textarea value set → button disabled kaliyor | Playwright + React 19 SPA | `page.evaluate` ile textarea value set etmek React fiber state'ini GUNCELLEMEZ → Send button disabled kalir. FIX: Playwright native `browser_type` + `slowly:true` kullan — React synthetic event handler'lari tetikler. `evaluate` SADECE okuma icin | cross-skill 2026-04-05 | [H] |
| Sidebar chat "Conversation not found: sidebar-..." 404 | OwnPilot gateway + sidebar chat | Gateway chat.ts `loadConversation(convId)` → memory'de yok → DB'de yok → 404. Sidebar yeni `sidebar-*` convId gonderiyor. FIX: `sidebar-` prefix ise `createWithId()` ile yeni conversation olustur. chat.ts L302 | cross-skill 2026-04-06 | [H] |
| Provider selector "Select provider" / "Auto" — gercek adi gostermiyor | OwnPilot UI sidebar + ana chat | Provider UUID id ile saklanir, `providers` listesi henuz fetch edilmemis → isim bulunamiyor. FIX: ChatPage'in `ownpilot-provider-names` localStorage cache'ini oku (providerNamesCache pattern) | cross-skill 2026-04-06 | [M] |
| Sidebar + ana chat ayni endpoint ama izole state | OwnPilot contextual chat mimarisi | Ikisi de POST /api/v1/chat kullaniyor (ayni pipeline). Fark: sidebar convId prefix `sidebar-*`, pageContext body field, ayri React Context (useSidebarChat vs useChatStore). Sidebar'dan gonderilen mesaj ana chat'i ETKILEMEZ | cross-skill 2026-04-06 | [M] |
